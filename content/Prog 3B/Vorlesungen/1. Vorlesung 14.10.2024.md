std::cout << "Hello World!" << std::endl;
std:: 
→ namespace
→ Methoden kommen in ein Namespace rein
→ std für Standard → Standard Namespace
*Namespace*::

<< → Nimmt Datentyp von rechts und schiebe es in den cout
std::endl wie println oder \\n
<mark style="background: #FF5582A6;">std::cout</mark> Datenstream

Strukturen → Ähnlich wie Objekte genutzt zur Datenkapselung

C-style (Pointer):
```C
typedef struct  {
	char *make; //pointer muss auf Speicherbereich zeigen für char
	char *model;
	int year; //int sind 32bit groß also kein Bedarf für Pointer
} Camera;

void printCamera(const Camera *cam) {
	cout <<cam-> make << ", " << cam->model << " (" << cam->year << endl;
}

int main {
	Camera camera1;
	camera1.make = "Leica";
	camera1.model = "M5";
	camera1.year = 1975;

	printCamera(&camera1); // & speichert Adresse -> Zeiger
}


```

Cpp-style (Referenz):
```C++
void printCamera(const Camera &cam) {
	cout <<cam-> make << ", " << cam->model << " (" << cam->year << endl;
}

int main {
	Camera camera1;
	camera1.make = "Leica";
	camera1.model = "M5";
	camera1.year = 1975;

	printCamera(camera1); // & fällt weg
}
```


## Programm als komplette Cpp-Struktur

```Cpp
/*
Schreiben Sie nun das Programm um, sodass die Kamera als C++ Struktur definiert wird.
Nutzen Sie statt char* den Typen std::string.
Initialisieren Sie die Kamera-Objekte durch einen Initialisierungsliste.
Implementieren Sie eine Member-Funktion toString(), welche eine String-Repräsentation der Kamera zurückgibt.
Nutzen Sie toString() in der main-Funktion, um die Kamera-Objekte auszugeben.
*/
#include <iostream>
#include <sstream>

using namespace std;

struct Camera{
	std::string make;
	std::string model;
	int year;

	string toString(){
		stringstream stream; //wie cout Datenstream
		stream << make << ", " << model << " (" << year << ") " << endl;
		return stream.str();
	}

	void print() {
	cout << make << ", " << model << " (" << year << ") " << endl;
	}
};

int main(){
	//Initialisierungsliste
	Camera camera1{"Leica", "M5", 1975};
	Camera camera2{"Canon", "EOS 5D", 2010};

	cout << camera1.toString();
	cout << camera2.toString();
}
```



```cpp
/*
Schreiben Sie nun das Programm um, sodass die Kamera als C++ Klasse definiert wird.
Achten Sie auf die Sichtbarkeit der Member-Variablen.
Implementieren Sie Konstruktoren (mit und ohne Parameter) und einen Destruktor.
Experimentieren Sie mit der Reihenfolge der Aufrufe der Konstruktoren und Destruktoren, indem Sie Objekte erzeugen und wieder zerstören.
Überladen Sie den << Operator, sodass eine Kamera auf der Konsole ausgegeben werden kann.
*/
#include <iostream>
#include <sstream>

using namespace std;

class Camera{
	std::string make; //standardmäßig private
	std::string model;
	int year;

	public: //mit expliziter Anweisung auf public setzen
	
	Camera(std::string make, std::string model, int year) //Konstruktor
		: make(make), model(model), year(year) {
		// this->make = make;    //this-Zeiger zum Zuweisen
		// this->model = model;
		// this->year = year;
	}
	string toString(){
		stringstream stream; //wie cout Datenstream
		stream << make << ", " << model << " (" << year << ") " << endl;
		return stream.str();
	}

	void print() {
	cout << make << ", " << model << " (" << year << ") " << endl;
	}
};
  
int main(){
	Camera camera1 {"Leica", "M5", 1975};
	Camera camera2 {"Canon", "EOS 5D", 2010};
	Camera camera3 ("Sony", "A6000", 2011); //auch so möglich mit Konstruktor anzulegen

	cout << camera1.toString();
	cout << camera2.toString();

	return 0;
}
```
